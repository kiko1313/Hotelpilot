import { ComingSoon } from "@/components/coming-soon";

export default function Page() {
  return (
    <ComingSoon
      title="Current Shift"
      stage="Stage 5"
      description="What's happening on the shift in progress right now: check-ins, check-outs, payments, notes."
    />
  );
}
